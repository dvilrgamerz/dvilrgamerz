# VaultBudget Status - 2026-06-04

Local production source:

`C:\Users\dhyey\OneDrive\Documents\save`

Current clean packages:

- `publish\vaultbudget-source.zip`
- `publish\vaultbudget-netlify-dist.zip`

Latest verified release gate:

- `npm run verify:release` passes.
- `npm run verify:publish` passes with 46 checks.
- Supabase project `v898` / `hykvbnqsslpfjpsplnwg` is active and healthy.
- Supabase cloud sync remains manual-first by default.
- Settings and auth panel show last local save time before login.
- Duplicate manual cloud Save is skipped when cloud data is already current and reports `0 rows`.
- The Check action is read-only and does not create Supabase rows.
- Source package excludes `.env.local`, `node_modules`, `dist`, and `publish`.

Still blocked outside the local app:

- Create or authorize a real `dvilrgamerz/vaultbudget` repo for full source upload.
- Complete Netlify login or manually upload `vaultbudget-netlify-dist.zip` through Netlify Drop.
- Enable Supabase leaked-password protection in the dashboard.
- Confirm a test user's email, sign in, press Save, then Check to create and verify the first private cloud rows.
